diff --git a/dist/content/vite-plugin-content-virtual-mod.js b/dist/content/vite-plugin-content-virtual-mod.js
index 021adef..de35dc3 100644
--- a/dist/content/vite-plugin-content-virtual-mod.js
+++ b/dist/content/vite-plugin-content-virtual-mod.js
@@ -48,21 +48,23 @@ function invalidateAssetImports(viteServer, filePath) {
   }
 }
 function invalidateDataStore(viteServer, { notifyClient = true } = {}) {
-  const environment = viteServer.environments[ASTRO_VITE_ENVIRONMENT_NAMES.ssr];
-  const module = environment.moduleGraph.getModuleById(RESOLVED_DATA_STORE_VIRTUAL_ID);
-  if (module) {
-    const timestamp = Date.now();
-    environment.moduleGraph.invalidateModule(module, void 0, timestamp, true);
-  }
-  if (isRunnableDevEnvironment(environment)) {
-    const runnerModule = environment.runner.evaluatedModules.getModuleById(
-      RESOLVED_DATA_STORE_VIRTUAL_ID
-    );
-    if (runnerModule) {
-      environment.runner.evaluatedModules.invalidateModule(runnerModule);
+  const timestamp = Date.now();
+  for (const environment of Object.values(viteServer.environments)) {
+    if (environment.name === ASTRO_VITE_ENVIRONMENT_NAMES.client) continue;
+    const module = environment.moduleGraph.getModuleById(RESOLVED_DATA_STORE_VIRTUAL_ID);
+    if (module) {
+      environment.moduleGraph.invalidateModule(module, void 0, timestamp, true);
+    }
+    if (isRunnableDevEnvironment(environment)) {
+      const runnerModule = environment.runner.evaluatedModules.getModuleById(
+        RESOLVED_DATA_STORE_VIRTUAL_ID
+      );
+      if (runnerModule) {
+        environment.runner.evaluatedModules.invalidateModule(runnerModule);
+      }
     }
+    environment.hot?.send("astro:content-changed", {});
   }
-  environment.hot.send("astro:content-changed", {});
   if (notifyClient) {
     viteServer.environments.client.hot.send({
       type: "full-reload",
diff --git a/dist/vite-plugin-app/index.js b/dist/vite-plugin-app/index.js
index 83cb7ca..09fa217 100644
--- a/dist/vite-plugin-app/index.js
+++ b/dist/vite-plugin-app/index.js
@@ -1,6 +1,6 @@
 const ASTRO_APP_ID = "virtual:astro:app";
 const RESOLVED_ASTRO_APP_ID = "\0" + ASTRO_APP_ID;
-const ASTRO_DEV_SERVER_APP_ID = "astro:server-app";
+const ASTRO_DEV_SERVER_APP_ID = "virtual:astro:server-app";
 function vitePluginApp() {
   let command;
   return [
